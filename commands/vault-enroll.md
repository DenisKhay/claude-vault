---
description: Manually register the current repo as a vault subgraph
---

Invoke the `vault` skill in **enroll** mode. Detect the current repo's identity (git remote, repo name, cwd), ask the user where the new subgraph should live, scaffold the folder, and append the registry entry. Confirm enrollment when done.
