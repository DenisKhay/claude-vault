---
description: Manually invoke the vault skill in actualize mode to capture session knowledge into the matched subgraph
---

Invoke the `vault` skill in **actualize** mode now. Sweep this session's deltas into the matched subgraph. Report the list of created and updated nodes when done.
